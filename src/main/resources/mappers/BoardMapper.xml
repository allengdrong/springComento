<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.org.comento.mapper.BoardMapper">

    <!-- 1. 게시판 리스트 -->
    <select id="list" resultType="com.org.comento.domain.BoardVO">
        select rnum, no, title, writer, writeDate
        from(
            select rownum rnum, no, title, writer, writeDate
            from (
            select no, title, writer, writeDate
            from tbl_board
            order by no desc
            )
            )
        where rnum between 1 and 10
    </select>

</mapper>